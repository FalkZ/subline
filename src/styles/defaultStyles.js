import { css } from "./css";
import { setDefaultStyles } from "./style";

// all: initial
setDefaultStyles(
  css`
    all: unset;
    all: initial;
    font-family: sans-serif;
    box-sizing: border-box;
  `
);
